using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class DeleteName : MonoBehaviour
{
    // Start is called before the first frame update

    public TextMeshPro speakerText;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
